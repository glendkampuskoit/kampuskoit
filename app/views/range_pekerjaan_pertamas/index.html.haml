- provide(:title, "Data Range Pekerjaan Pertama")
.row-fluid
  .span12
    = link_to "Tambah Range Pekerjaan Pertama", new_range_pekerjaan_pertama_path, :role => "button", :class => "btn btn-mini btn-primary"
.row-fluid
  .span12
    .box.corner-all
      .box-header.grd-white.corner-top
        .header-control
          %a{"data-box" => "collapse"}
            %i.icofont-caret-up
          %a{"data-box" => "close", "data-hide" => "bounceOutRight"} &times;
        %span Data Range Pekerjaan Pertama
      .box-body
        %table#datatables.table.table-hover.responsive
          %thead
            %tr
              %th Pekerjaan Pertama
              %th &nbsp;
          %tbody
            - @range_pekerjaan_pertamas.each do |pekerjaan_pertama|
              %tr
                %td= pekerjaan_pertama.pekerjaan_pertama
                %td.center
                  %span.label.label-info
                    = link_to "Edit", edit_range_pekerjaan_pertama_path(pekerjaan_pertama)
                  %span.label.label-important
                    = link_to "Delete", range_pekerjaan_pertama_path(pekerjaan_pertama), method: :delete, :class => "delete"

:javascript
  $(document).ready(function() {
      // try your js
      
      // peity chart
      $("span[data-chart=peity-bar]").peity("bar");
      
      // datatables
      $('#datatables').dataTable( {
          "sDom": "<'row-fluid'<'span6'l><'span6'f>r>t<'row-fluid'<'span6'i><'span6'p>>",
          "sPaginationType": "bootstrap",
          "oLanguage": {
                  "sLengthMenu": "_MENU_ records per page"
          }
      });

      $('.delete').on('click', function () {
          return confirm('Anda yakin akan menghapus data ini?');
      });
  });