- provide(:title, "Data Range Gaji Pertama")
.row-fluid
  .span12
    = link_to "Tambah Range Bidang Kerja", new_range_bidang_kerja_path, :role => "button", :class => "btn btn-mini btn-primary"
.row-fluid
  .span12
    .box.corner-all
      .box-header.grd-white.corner-top
        .header-control
          %a{"data-box" => "collapse"}
            %i.icofont-caret-up
          %a{"data-box" => "close", "data-hide" => "bounceOutRight"} &times;
        %span Data Range Gaji Pertama
      .box-body
        %table#datatables.table.table-hover.responsive
          %thead
            %tr
              %th Gaji Pertama
              %th &nbsp;
          %tbody
            - @range_gaji_pertamas.each do |range_gaji_pertama|
              %tr
                %td= range_gaji_pertama.gaji_pertama
                %td.center
                  %span.label.label-info
                    = link_to "Edit", edit_range_gaji_pertama_path(range_gaji_pertama)
                  %span.label.label-important
                    = link_to "Delete", range_gaji_pertama_path(range_gaji_pertama), method: :delete, :class => "delete"

:javascript
  $(document).ready(function() {
      // try your js
      
      // peity chart
      $("span[data-chart=peity-bar]").peity("bar");
      
      // datatables
      $('#datatables').dataTable( {
          "sDom": "<'row-fluid'<'span6'l><'span6'f>r>t<'row-fluid'<'span6'i><'span6'p>>",
          "sPaginationType": "bootstrap",
          "oLanguage": {
                  "sLengthMenu": "_MENU_ records per page"
          }
      });

      $('.delete').on('click', function () {
          return confirm('Anda yakin akan menghapus data ini?');
      });
  });