require "spec_helper"

describe SubscriberMailer do
	# before { @subscriber = Subscriber.new(name: "Glend Maatita", email: "glend_maatita@yahoo.com", password: "foobar", password_confirmation: "foobar") }

 #  	describe "Send Email when user sign up with email" do
 #  		let(:sign_up_mailer) { SubscriberMailer.welcome_email(@subscriber) }

 #  		it { sign_up_mailer.from.should == [Rails.application.config.email]  }
 #  		it { sign_up_mailer.to.should == [@subscriber.email]  }
 #  	end
end
